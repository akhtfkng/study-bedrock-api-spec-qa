# API Spec QA Agent (PoC) environment examples

# Optional: configure Amazon Bedrock for LLM-based formatting.
# When unset, the agent falls back to deterministic answers.
BEDROCK_REGION=ap-northeast-1
BEDROCK_MODEL_ID=anthropic.claude-sonnet-4-20250514-v1:0

# Natural-language search tunables (these values are defaults if unset)
SEARCH_SCORE_THRESHOLD=0.2
SEARCH_TOP_K=3

EMBEDDINGS_ENABLED=true
EMBED_WEIGHT=0.6          # 任意。0〜1 の範囲で BM25 と埋め込みの比率を調整
USE_LLM=true           # 任意。true に設定すると LLM ベースの応答生成を使用
